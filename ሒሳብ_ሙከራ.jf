// ሒሳብ_ሙከራ.jf - Testing logic and arrays
ይሁን ቁጥሮች = [፩, ፪, ፫, ፬, ፭, ፮, ፯, ፰, ፱, ፲]፤
ይሁን የኢትዮጵያ_ቀን = አዲስ ቀን()፤

ያትም("የዛሬ ቀን፦ " + የኢትዮጵያ_ቀን።toDateString())፤

ለእያንዳንዱ (ይሁን ቁጥር የ ቁጥሮች) {
    ከሆነ (ቁጥር % ፪ === ፟) {
        ያትም(ቁጥር + " - ተጋማሽ (Even)")፤
    } ካልሆነ {
        ያትም(ቁጥር + " - ኢ-ተጋማሽ (Odd)")፤
    }
}

ምረጥ (ቁጥሮች።length) {
    ሁኔታ ፲:
        ያትም("አስር ቁጥሮች አሉ።")፤
        ውጣ፤
    መደበኛ:
        ያትም("ቁጥሩ አይታወቅም።")፤
}